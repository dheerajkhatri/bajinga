#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    string convert(string s, int numRows) {
		string str = "";
		int len = str.length();

		for(int i=0;i<numRows;i++){
			int cur = i;

		}
    }
};

int main(){

	return 0;
}